- meta_info = dapei_page_seo(obj)
- content_for :title do
  = meta_info['title']

- content_for :description do
  = meta_info['desc']

- if meta_info['keywords']
  - content_for :keywords do
    = meta_info['keywords']
