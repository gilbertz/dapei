# encoding: utf-8
#!/usr/bin/env ruby

require "rubygems"

<% num = 1%>

#while true do 
<% @spiders.each do |s| %>
    p "<%=num %>"
    <% num += 1%> 
    `cd /data/spider && /bin/bash spider.sh <%= s.id %> > /tmp/run_spider_<%= s.id %>.log`
<% end %>
#end
